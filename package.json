{
  "name": "json-server-products",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon --watch db.json server.js",
    "start": "nodemon --watch db.json server.js",
    "build": "nodemon --watch db.json server.js"
  },
  "dependencies": {
    "clone": "^2.1.2",
    "json-server": "^0.17.1",
    "nodemon": "^2.0.21"
  }
}
